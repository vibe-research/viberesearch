# Current methodology

Updated: {{DATE}}
Active protocol: none. Establish the initial protocol before running research.

When agreed, link to one version here, for example a file named
`m-001-baseline.md` in this directory. Record why it is current and when it took
effect. Each experiment pins its own version directly, preserving past methods
when this pointer changes. A newly generated protocol remains a draft until its
contents and activation are supported by the researcher's direction.
